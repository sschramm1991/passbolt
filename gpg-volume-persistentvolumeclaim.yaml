apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: passbolt-gpg-pvc
spec:
  storageClassName: "local-storage"
  volumeName: passbolt-gpg-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
status: {}
